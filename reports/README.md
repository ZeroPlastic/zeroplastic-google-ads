# reports

Performance analysis and reporting. Once live Google Ads API access is configured, these are generated directly from account data pulled by Claude; until then, they're built from data the user supplies (e.g., exported CSVs from Google Ads or Google Analytics).

Suggested organization: one file per reporting period, e.g. `reports/2026-07-monthly-summary.md`.
